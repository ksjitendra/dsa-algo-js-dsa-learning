//   typeof() - Returns the type of a variable or expression as a string. Useful for type checking and debugging.

parseInt(), parseFloat(); //- Converts a string to an integer or floating-point number, respectively.

// String() - Converts a value to a string data type. Can be used to concatenate or manipulate strings.

// Number() - Converts a value to a number data type. Useful for converting strings to numbers.

// Math.random() - Generates a random floating-point number between 0 and 1. Can be combined with other Math functions to generate random numbers within a desired range.

// Math.floor(), Math.ceil(), and Math.round() - Functions for rounding numbers down, rounding numbers up, and rounding numbers to the nearest whole number, respectively.

// Math.max() and Math.min() - Returns the maximum or minimum value from a set of numbers or arguments.

// Array.prototype.length - Returns the number of elements in an array.

// Array.prototype.push() - Adds one or more elements to the end of an array and returns the new length of the array.

// Array.prototype.pop() - Removes the last element from an array and returns that element.

// Array.prototype.join() - Joins all elements of an array into a string, with an optional separator between elements.

// Array.prototype.slice() - Returns a shallow copy of a portion of an array into a new array, without modifying the original array.

// Array.prototype.splice() - Changes the contents of an array by removing, replacing, or adding elements.

// Array.prototype.indexOf() - Returns the first index at which a given element can be found in an array, or -1 if it is not present.

// Array.prototype.forEach() - Executes a provided function once for each array element.

// Array.prototype.map() - Creates a new array with the results of calling a provided function on every element in the original array.

// Array.prototype.filter() - Creates a new array with all elements that pass a test implemented by a provided function.

// Array.prototype.reduce() - Applies a function against an accumulator and each element in the array (from left to right) to reduce it to a single value.

// Date() - Returns the current date and time.

// Date.prototype.getTime() - Returns the numeric value corresponding to the time for a specified date as the number of milliseconds since January 1, 1970.
